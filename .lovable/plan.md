In `src/routes/index.tsx`, the kiosk tab buttons currently left-align their label text. Add `justify-center` to the tab button class and `text-center` plus `w-full` to the label `<span>` so the label is centered within each fixed-width tab.